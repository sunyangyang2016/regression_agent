[project]
name = "agent"
version = "0.2.0"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.28.1",
    "openai>=2.45.0",
    "psutil>=5.9.0",
    "yt-dlp>=2024.1.0",
]

[tool.uv.workspace]
members = [
    "mcp_server/server/uweather",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
